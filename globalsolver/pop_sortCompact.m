function [pop] = pop_sortCompact(pop)
% auxiliary function
% this function sort evoluation popluation w.r.t.
% number of objectives
% constraints
% nd sort is not compatible with single objective problems
%-----------------------------------------------------

num_fields = length(fieldnames(pop));


end
